# UniFi Network API MCP Server Configuration
# Copy this file to .env and update with your values

# Required: UniFi Controller URL (without trailing slash)
UNIFI_CONTROLLER_URL=https://your-controller.unifi-hosting.ui.com

# Required: UniFi API Key for authentication
UNIFI_API_KEY=your-api-key-here

# Optional: API request timeout in seconds (default: 30.0)
# UNIFI_API_TIMEOUT=30.0

# Optional: Number of retry attempts for failed requests (default: 3)
# UNIFI_API_RETRY_ATTEMPTS=3

# Optional: Delay between retry attempts in seconds (default: 1.0)
# UNIFI_API_RETRY_DELAY=1.0

# Optional: Default page size for paginated requests (default: 25)
# UNIFI_DEFAULT_PAGE_SIZE=25

# Optional: Logging level (default: INFO)
# UNIFI_LOG_LEVEL=INFO